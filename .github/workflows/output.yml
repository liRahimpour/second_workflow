name: output info
on: [workflow_dispatch]
jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: output github context
        run: echo "${{ toJSON(github) }}"
      - name: echo
        run: echo "dispatch"
